# SQLite database file path
DATABASE_URL=file:./data/vital.db

# Directory for processed (WebP) images
UPLOADS_DIR=./data/uploads

# bcrypt hash (cost 11) of the admin password — generate with bcryptjs, never commit the plaintext
# IMPORTANT: Vite's env loader runs dotenv-expand on this file, which treats an
# unescaped `$name` as a variable reference and silently deletes it if undefined
# (this WILL corrupt a bcrypt hash, which is full of `$`). Escape every `$` as `\$`:
#   ADMIN_PASSWORD_HASH=\$2b\$11\$abcdefghijklmnopqrstuv.wxyz0123456789ABCDEFGHIJKLMNOPQR
ADMIN_PASSWORD_HASH=

# Random string >= 32 chars used to sign the session cookie
SESSION_SECRET=

# Canonical site URL, used for SEO/sitemap
PUBLIC_SITE_URL=http://localhost:4321
